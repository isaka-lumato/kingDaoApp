# Project Status — KDL Tracker

**Live state of the project.** Updated after every task.

---

## Snapshot

| Field | Value |
|---|---|
| **Phase** | **Phase 5 closed**, **Phase 6 closed** as of 2026-05-31 (T-072 PDF export shipped — both report exports now ship). **Perf sweep landed mid-Phase-6** (D-040 → D-044): middleware fast path, cross-request permissions cache, `/consignments` parallelization, `useTransition` on the filter bar, `/settings/users` inner+outer parallelization. **2026-05-28**: Triage view (D-045) landed — replaces kanban as the mobile default, adds desktop tab. T-086 + T-080 done. Phase 4's T-053b (alerts deploy) still parked pending Supabase dashboard access. Next: **Phase 7 close-out** (T-081 security review → T-082 CI → T-083 deploy) + ad-hoc QA on `/reports` exports + `/import`. |
| **Last updated** | 2026-05-31 |
| **Last task completed** | T-081 — security review. RLS coverage audit (`audit_rls.sql`) confirms RLS on all 14 user-facing tables + no permissive hard-DELETE on operational tables; secret-key isolation verified (`server-only` guard + dep, no client-component imports, admin-client allowlist = 3 D-026 sites, zero hardcoded keys). **Closed the documented compromise (D-046):** the `consignments_update` policy was `with check (true)` (per-column writes app-layer only) — added migration `20260525090000_consignments_column_write_guard.sql`: a BEFORE UPDATE trigger calling `can_user_write()` per changed column (raises `42501`), with a tx-local GUC bypass so `advance_stage()`/`force_set_stage()` still work; admins short-circuit. Folded-in: fixed the `in_ref_batch_id`→`in_ref` seed/UI typo. Applied to dev via `db push` (after repairing pre-existing import_jobs migration-history drift), types regenerated. 3-agent audit + source verification rejected several false-positive "critical" flags. Behavioural REST walkthrough owed to ad-hoc QA. New T-087 logged for the identical `efd_records` gap. Gates: typecheck clean, lint 0 errors / 5 warnings, 67/67 tests. |
| **Prior task completed** | T-072 — PDF export for all 6 reports (Phase 6 close). New `src/server/reports/build-pdf.tsx` (pure react-pdf builder, mirrors `build-xlsx.ts`): A4 landscape, fixed page header with the Kingdao logo + title + `Generated … UTC` + filter summary repeated on every page, footer `page / total` counter, per-report tables switched on `payload.kind`, money via `formatTzs`, dates `yyyy-mm-dd`, totals row on Revenue / Client Volume / Pipeline Bottleneck / Pending Refunds, empty-result sentinel, `wrap={false}` data rows + repeated table header on page breaks. Route handler `src/app/api/reports/[kind]/pdf/route.ts` (`runtime: "nodejs"`, `force-dynamic`) mirrors the XLSX route's auth/query shape and reads the same `fetchReportRows()`. Page wiring: each `ReportHeader` now gets `exportHrefPdf`; stale "PDF export is tracked as T-072" footer note dropped. `tests/unit/build-pdf.test.ts` (+9 cases — each kind renders a valid `%PDF`; empty/null payloads don't throw). One `jsx-a11y/alt-text` false positive on react-pdf's `<Image>` suppressed with a targeted disable. D-026 allowlist stays at 3 sites; no SheetJS/exceljs in the PDF path. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 67/67 unit tests, `pnpm build` clean with `/api/reports/[kind]/pdf` in the route manifest. |
| **Current task in progress** | None — T-081 done. Phase 7 close-out continues: **T-082** (CI on GitHub Actions) is the next codeable task, then T-083 (deploy). |
| **Blocked tasks** | None |
| **Production deployed?** | No |
| **Active branch** | `trial` (not yet merged to `main`) |
| **Approach** | Cloud-only Supabase per D-019; flat Next.js repo per D-021 |
| **Stack confirmed running** | Next 16.2 · React 19.2 · Tailwind 4 · TypeScript 5 · Supabase JS 2.106 · zod 4.4 · date-fns 4.2 · Vitest 4.1 · Playwright 1.60 |

---

## What exists today

### Planning
- `PRD.md` (v1, frozen)
- `CLAUDE.md`, `tasks.md`, `validation.md`, `decisions.md` (D-001..D-028), `status.md`, `humanTasks.md`

### Code
- Next.js 16 app at repo root (App Router, TypeScript, Tailwind 4)
- shadcn/ui initialized on Base UI primitives (`base-nova` preset)
- `src/lib/supabase/{env,client,server,admin,middleware}.ts` — full client/server/admin/middleware wiring
- `src/middleware.ts` — Supabase session refresh + route protection (T-031)
- `src/lib/{money,dates,query-keys,pipeline,permissions}.ts` — shared utilities; pipeline state machine constants split out of server actions (D-027)
- `src/schemas/{common,auth}.ts` — zod fragments
- `src/types/supabase.ts` — **generated from live dev DB** (regenerate after each migration)
- `src/hooks/use-permissions.tsx` + `src/components/auth/permission-gate.tsx` — client-side perm hook + gate
- `supabase/config.toml` — CLI initialized and **linked to kdl-tracker-dev**
- `supabase/migrations/` — 14 migrations applied to dev (Phase 1 + RLS for reference tables + Phase 4 follow-ups):
  - `175744` helpers + audit log
  - `175800` enums (T-010)
  - `175810` clients + ICDs + seed (T-011)
  - `175820` roles, permissions, system role seed (T-018/T-019) + audit fix
  - `005000` consignments table (T-012)
  - `005200` efd_records + efd_record_consignments (T-014)
  - `005250` v_in_ref_batches view (T-013)
  - `005300` guta_pairs + auto-pair trigger (T-015)
  - `005400` stage_history + settings singleton (T-016/T-017)
  - `005500` advance_stage() + force_set_stage() (T-021/T-022)
  - `005600` v_stuck_stages + 6 reporting views (T-023/T-024)
  - `025325` RLS SELECT/WRITE policies for `clients` and `icds` (needed for dropdowns + joins)
  - `20260522004757` advance_stage caller-role check (T-048 follow-up, D-029)
  - `20260523163840` `stuck_alerts` table + `claim_new_stuck_alerts()` + `reset_resolved_stuck_alerts()` (T-053a)
- **Phase 2 (auth)** — `(auth)/login`, middleware route protection, server-side `getServerPermissions()`, `PermissionGate`, Settings → Users (invite/role/deactivate), Settings → Roles (clone + per-column matrix)
- **Phase 3 (CRUD)** — Kanban (`/`) with DnD + `advance_stage` RPC + admin force-set dialog; Action Inbox (`/inbox`); Consignments table (`/consignments`) with filter set + pagination; Detail (`/consignments/[id]`, 3-tab, now with Linked-EFDs section); New form; Edit form (column-perm enforced); Duplicate action; Soft-delete with reason
- **Phase 4 (EFD)** — EFD management at `/efd` (list with flag filter + code search + pagination), `/efd/new` (form + consignment picker + unreleased soft-warning + batch-prefill from `?from_batch=`), `/efd/[id]` (edit + add/remove links + admin-only delete). Server actions in `src/server/actions/efd.ts`. Schema `src/schemas/efd.ts`. Flags derived from code on the server; `is_shared` recomputed on every link change. **EFD auto-link to `in_ref` siblings** (PRD §8.4 line 433) via `expandToBatchSiblings()` in both `createEfdAction` and `linkConsignmentsAction`.
- **Phase 4 (in_ref)** — Batch panel drawer at `/consignments` and `/consignments/[id]` driven by `?batch=…&bc=…&by=…` URL params. Built from `_batch-panel/batch-panel.tsx` (client shell with ESC + backdrop close) + `_batch-panel/batch-panel-content.tsx` (server component fetching `v_in_ref_batches` + sibling rows). `BatchLink` chip wired into the consignments table (new IN REF column) and the consignment detail Core details section.
- **Phase 4 (GUTA pair)** — Consignment detail Overview tab now shows a "GUTA pair" section when `guta_pair_id` is set. `consignments/[id]/page.tsx` resolves the sibling via `guta_pairs` + a non-deleted consignment fetch; `consignment-detail.tsx` renders the indigo batch-code badge, a clickable sibling card (REF No, B/L, container count × type, amount, release status, release date), and a red warning banner when exactly one of the pair is released (PRD §8.15).
- **Phase 4 (Dashboard)** — `/dashboard` (server component) with 4 KPI tiles (Released today, Pending release, Stuck > 48h, Revenue · current month), pipeline funnel (10 Action stages from `v_pipeline_funnel`), top 5 clients by container volume (`v_client_volume`), arrivals this week (Mon→Sun window from `consignments`), and overdue jobs top-10 (`v_stuck_stages`). 7 reads dispatched in parallel via `Promise.all`. Sidebar nav reordered: Dashboard (`/dashboard`) → Pipeline (`/`, was "Dashboard") → Consignments → Inbox → EFD → Reports → Settings.
- **Phase 4 (Alerts — code only, T-053a)** — Migration `20260523163840_stuck_alerts.sql` (table + two SECURITY DEFINER helpers); edge function `supabase/functions/alerts/index.ts` (Deno, bearer-token gate, calls reset + claim, resolves admin emails via auth.admin API, POSTs digest email per admin to Resend HTTP API directly per D-033). Deploy + cron schedule pending in H-010 (T-053b).
- **Phase 6 (Reports, T-070)** — `/reports` server component dispatches on `?report=…&year=…&from=…&to=…` and renders the matching one of 6 sub-components (Revenue / Client Volume / Turnaround · by Client / Turnaround · by ICD / Pipeline Bottleneck / Pending Refunds), each backed by its T-024 view. `reports-filter-bar.tsx` is a client component using `useTransition` for non-blocking nav. Year selector defaults to current year ± 3. Date inputs are disabled with an inline explanation on year-grain reports (D-039). Each report shows a totals row where it makes sense (Revenue, Client Volume, Pending Refunds, Pipeline Funnel %). Money via `formatTzs`, dates via `formatDate`. All queries use `getSupabaseServerClient` — D-026 allowlist stays at 3 sites. As of T-071 the per-report queries are dispatched through `fetchReportRows()` in `src/server/reports/fetch-report-rows.ts` so the rendered table and the XLSX export read identical bytes.
- **Phase 6 (XLSX export, T-071)** — Each report card on `/reports` gained an **Export XLSX** anchor in its header (plain `<a href download>` so the browser handles the download UI; no JS). Route handler at `src/app/api/reports/[kind]/xlsx/route.ts` (`runtime: "nodejs"`, `dynamic: "force-dynamic"`): validates `kind` against `REPORT_OPTIONS`, gates on `getServerPermissions()` (401 for unauth — viewers + up can read), parses `year` / `from` / `to` (re-uses the page's ISO-date validator), runs `fetchReportRows()`, then `buildReportWorkbook()` → `workbook.xlsx.writeBuffer()` → `Response` with `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` + `Content-Disposition: attachment; filename="kdl-<kind>-<year>[-<from>-<to>].xlsx"`. Workbook layout: row 1 title (e.g. `Revenue Summary · 2026`), row 2 generated-at + filter summary, row 3 blank, row 4 bolded + frozen headers, row 5+ data, totals row where the page has one. **Money cells are numeric `numFmt = "TSh"#,##0`** (not formatted strings — sortable, summable). **Date cells are real `Date` with `numFmt = yyyy-mm-dd`**. Empty result writes a single sentinel cell `"No rows matched the filter."` at row 5 — never throws. Sheet names cleaned for Excel (strip `\ / ? * [ ] : ·`, cap at 31). New: `src/server/reports/{fetch-report-rows,build-xlsx,report-types}.ts`, `src/app/(app)/reports/report-options.ts` (extracted `REPORT_OPTIONS` + `ReportKind` out of the `"use client"` filter-bar so server code can import them without crossing the client-reference-proxy boundary that broke the first build attempt), and `src/app/api/reports/[kind]/xlsx/route.ts`. Modified: `src/app/(app)/reports/page.tsx` (replaced 6 inline view-fetch blocks with `fetchReportRows()`; added the Export XLSX anchor to each `ReportHeader`; dropped the T-071 mention from the footer note). `reports-filter-bar.tsx` re-exports `REPORT_OPTIONS` + `ReportKind` for backward compat. `exceljs@4.4.0` added (server-only — used only from the route handler and the workbook builder). D-026 allowlist stays at 3 sites (no admin-client uses in any new code). D-035 invariant holds (no SheetJS import in new code). V-REPORTS-XLSX added to `validation.md`. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, **46/46 unit tests** (was 35/35; +11 in `tests/unit/build-xlsx.test.ts`), `pnpm build` clean with `/api/reports/[kind]/xlsx` in the route manifest.
- **Phase 5 (CLI importer, T-062)** — `scripts/import-tracker.ts` (~520 lines, run via `pnpm import:tracker <file>`). Admin-client auth (service key from `.env.local`, same pattern as `scripts/create-viewer-user.mjs`) — bypasses RLS, designed for the initial bulk historical load. Dry-run by default; `--commit` requires typing `IMPORT` on stdin (skip with `--yes` for non-interactive runs). Project-label banner (`DEV` / `PROD` / `UNKNOWN`) compares `NEXT_PUBLIC_SUPABASE_URL` against the project IDs in `package.json`. Dry-run mode dumps a full `tmp/import-preview-<iso>.json` for offline review (`tmp/.gitignore` keeps those out of source). Commit branch creates one `import_jobs` row (`status='previewed'` → `'committed'`/`'failed'`) with `payload.source='cli'` for provenance, then mirrors `commitImportAction`'s row-by-row insert loop. `--no-auto-create` flag (CLI-only) fails fast if any client/ICD would need to be created. Exit codes: 0 clean / 1 setup error / 2 partial-failure commit. `tsx@^4.19.2` added to devDependencies.
- **Phase 5 (Excel import UI, T-061)** — `/import` route (server gate: admin+operator). `src/server/import/import-actions.ts` has `previewImportAction` (file → parser → `import_jobs.status='previewed'` row) and `commitImportAction` (re-parse file → resolve FKs with case-insensitive `ilike` + in-process cache → auto-create missing clients/ICDs → row-by-row `consignments` + `efd_records` insert → update audit row to `'committed'`). Sidebar entry "Import" between EFD and Reports. `xlsx@0.18.5` installed (server-only). Migration `20260524181824_import_jobs.sql` applied (audit table with RLS: admin SELECT all; operator SELECT own; admin+operator INSERT/UPDATE).
- **Phase 5 (Excel parser, T-060)** — `src/server/import/parse-tracker.ts` (pure module, no SheetJS dep). Public entry `parseTracker(rows: CellValue[][])` returns `{ consignments, errors, warnings, summary }`. Handles: year-separator rows (single 4-digit cell), header-driven column resolution (case + punctuation + whitespace tolerant), Excel-serial / dd-mm-yyyy / Date / ISO date conversion, decimal-time → HH:MM:SS, multi-EFD comma-split with `..NNN` abbreviated-prefix expansion, REF No left-pad to 7 digits with warning (§8.20), per-row pipeline enum coercion (unknown → Waiting + warning), and §8.5 cross-field soft validation (40FT/20FT/CAR/COIL amount bands, COIL→DP WORLD, CAR+in_ref). D-035 + D-036 logged.
- `tests/unit/{money,dates,parse-tracker}.test.ts` — 35/35 passing (was 7/7; +28 for parser)
- `tests/e2e/smoke.spec.ts` + `playwright.config.ts`
- `.env.local` (dev URL + publishable + secret key wired)

### Operational
- `fixtures/TRACKER -- KDL.xlsx` — historical data (gitignored)

---

## Recent activity

| Date | Event |
|---|---|
| 2026-05-18 | Planning artifacts created. Stack locked. Workflow style locked. Permission model locked. D-001..D-018 logged. |
| 2026-05-18 | D-019: switched to cloud-only Supabase (no local Docker). |
| 2026-05-18 | H-001 done — Node 24, pnpm 10.33, Git 2.54, Supabase CLI 2.98.2 verified. |
| 2026-05-18 | D-020: adopt new Supabase publishable/secret keys instead of legacy anon/service_role. |
| 2026-05-18 | H-002 done — dev + prod Supabase projects created. |
| 2026-05-18 | D-021: flat repo structure (no `apps/web/` monorepo). |
| 2026-05-18 | D-022: adopt Next.js 16 (current default), not 15 as originally planned. |
| 2026-05-18 | D-023: shadcn/ui on Base UI (the new shadcn default), not Radix. |
| 2026-05-18 | T-001 done — Next.js scaffold, typecheck + lint + dev all clean. |
| 2026-05-18 | T-002 done — shadcn/ui initialized, sample button renders. |
| 2026-05-18 | T-003 done — `supabase init` complete; `config.toml` committed. CLI link deferred to H-007. |
| 2026-05-18 | T-004 done — full Supabase client wiring (browser/server/admin/middleware); homepage SSR confirms env wiring works against dev project. |
| 2026-05-18 | T-005 done — money/date/query-keys utilities + zod schema fragments; deps installed. |
| 2026-05-18 | T-006 done — Vitest config + 7 passing unit tests; Playwright config + smoke spec. |
| 2026-05-19 | H-007 done — `supabase login` + `supabase link` to kdl-tracker-dev (vmkhiahoytuqnjpcxwrb). |
| 2026-05-19 | T-010 done — enums migration applied (`175800`). |
| 2026-05-19 | T-011 done — clients + icds tables applied (`175810`). |
| 2026-05-19 | T-018/T-019 done — roles, user_roles, role_column_permissions + system role seed applied (`175820`). Bugfix: audit_log.row_id made nullable for composite-PK tables. |
| 2026-05-19 | T-012 done — consignments table (all PRD §5 fields, FKs, RLS, audit trigger, uniqueness constraints). |
| 2026-05-19 | T-013 done — v_in_ref_batches view (computed, not a table, per D-012). |
| 2026-05-19 | T-014 done — efd_records + efd_record_consignments M:M join. |
| 2026-05-19 | T-015 done — guta_pairs table + auto_detect_guta_pair() trigger. |
| 2026-05-19 | T-016/T-017 done — stage_history + settings singleton + audit triggers. |
| 2026-05-19 | T-021/T-022 done — advance_stage() + force_set_stage() with full PRD §8 prerequisites. |
| 2026-05-19 | T-023/T-024 done — v_stuck_stages + 6 reporting views. |
| 2026-05-19 | T-025 done — supabase.ts regenerated from full Phase 1 schema. |
| 2026-05-19 | T-030..T-033 done — login, middleware route protection, permissions hook, PermissionGate. |
| 2026-05-19 | T-034 done — Settings → Users (invite/role/deactivate). Bugfix: switched invite-by-email to create-with-password to match Supabase v2 admin API. |
| 2026-05-19 | T-035 done — Settings → Roles + per-role × per-column permissions matrix. Phase 2 complete. |
| 2026-05-19 | T-040 done — Kanban board with DnD, `advance_stage` RPC, admin backward-move dialog. |
| 2026-05-19 | T-041/T-042 done — Action Inbox + Consignments table with filters and pagination. |
| 2026-05-19 | Bugfix — pipeline types extracted to `lib/pipeline.ts`; `"use server"` modules cannot export non-serializable values (see D-027). |
| 2026-05-19 | T-043/T-044 done — Consignment detail (3-tab) + new consignment form + `createConsignmentAction`. |
| 2026-05-19 | T-045 done — Edit form with column-level permission enforcement. |
| 2026-05-19 | Bugfix batch — dark-mode input contrast; sidebar logo; corrected `audit_log` column names. |
| 2026-05-19 | RLS bugfix — added SELECT policies for `clients` and `icds` (migration `025325`) so dropdowns + joins resolve. |
| 2026-05-19 | Schema bugfix — corrected `container_type` enum values to `40FT/20FT/CAR/COIL` and auto-generate `ref_no` + `serial_no` on insert (D-028). |
| 2026-05-19 | T-046/T-047 done — duplicate + soft-delete actions. Phase 3 complete. |
| 2026-05-20 | **Trial-branch audit by Baraka + Claude** — identified RLS-bypass-on-reads pattern across 7 server pages (see D-026); logged as T-048 cleanup. Identified perf regression from 3 serial `auth.getUser()` round-trips per request; logged as T-049. Documentation hygiene caught up (`status.md`, `decisions.md`, `tasks.md`, `validation.md`). |
| 2026-05-20 | T-048 code complete — swapped `getSupabaseAdminClient()` → `getSupabaseServerClient()` on 7 page files + `fetchKanbanData`. Added `.is("deleted_at", null)` on the `icds` reads that were missing it. Side-fix: replaced `<a href="/consignments/new">` with `<Link>` in `kanban-board.tsx` (was blocking the lint gate). Validation: `grep -rn getSupabaseAdminClient src/` returns only the 4 permitted call sites; typecheck + lint + tests all green. Manual viewer/operator/admin verification still owed before marking T-048 `[x]`. |
| 2026-05-22 | **D-029 logged + T-048 follow-up shipped.** Viewer walkthrough exposed that a viewer could drag kanban cards — root cause was `advance_stage()` being `SECURITY DEFINER` (bypasses RLS) without a caller-role check. Fixed in migration `20260522004757_advance_stage_role_check.sql` (applied to dev). UI guard added: `kanban-card.tsx` uses `useSortable({ disabled: !canDrag })` and the board's `handleDragEnd` refuses non-admin/operator. Verified via direct REST RPC with viewer JWT — returns `42501 Role admin or operator required`. Validation gates green. |
| 2026-05-22 | **T-048 closed on code-level acceptance.** Static gates: typecheck clean, lint 0 errors / 7 pre-existing unused-var warnings, 7/7 unit tests pass. Confirmed `getSupabaseAdminClient` is limited to the 4 permitted modules (`lib/supabase/admin.ts`, `settings-users.ts`, `settings-roles.ts`, `forceSetStageAction` in `consignments.ts`). Detail + edit pages filter `.is("deleted_at", null)` and `notFound()` on miss. V-PERM in `validation.md` already carries the RLS-bypass audit, soft-delete leak audit, and the D-029 `SECURITY DEFINER` caller-role gate. Operator + admin click-through deferred to ad-hoc QA per user direction. |
| 2026-05-22 | **T-049 done + D-030 logged.** (1) `(app)/layout.tsx` switched from `auth.getUser()` to `auth.getClaims()` (local JWT verify, no Auth-server RTT). (2) `getServerPermissions()` wrapped in React `cache()` — layout/page/actions in one request share one resolved permission set. (3) Role-lookup chain collapsed from 3 queries to 2 (joined `roles!inner(name)` into `role_column_permissions`). (4) Admin walkthrough surfaced a pre-existing bug: dragging cards *backwards* as admin failed with `force_set_stage requires admin role`. Root cause — `forceSetStageAction` called the RPC via the admin/service-role client, which made `auth.uid()` null inside the `SECURITY DEFINER` function and tripped its own `is_admin()` guard. Fixed by routing the RPC through the user-bound server client (server-action's `perms.isAdmin` check still runs first). This shrinks D-026's admin-client allowlist from 4 → 3 sites; `validation.md` V-PERM updated. **Measured:** `GET /` `application-code` time dropped from ~1.5–2s (pre-T-049 audit) to **31–169ms warm** on dev — past the ≥50% threshold by a wide margin. Gates green: typecheck clean, lint 0 errors / 7 pre-existing warnings, 7/7 unit tests, force-set RPC now returns `200` in the dev-server log. |
| 2026-05-23 | **T-050 done.** EFD management shipped — `/efd` (list with flag filter + code search + pagination), `/efd/new` (form + consignment multi-picker + amber soft-warning when linking an unreleased consignment per PRD §8.14), `/efd/[id]` (combined detail+edit with add/remove links + admin-only delete). Server actions in `src/server/actions/efd.ts`: create/update/delete + link/unlink. Flags hybrid model: PRIVATE/TRANSIT auto-derived from code via `normaliseFlagsFromCode` on the server; `is_shared` always recomputed from link count (≥ 2). Consignment detail (`consignments/[id]/page.tsx` + `consignment-detail.tsx`) now queries `efd_record_consignments → efd_records` and renders a "Linked EFD records" section on the Overview tab — acceptance criterion (one EFD on all linked consignments' detail views) met. New zod schema at `src/schemas/efd.ts`. V-EFD added to `validation.md`. Gates: typecheck clean, lint 0 errors / 7 pre-existing warnings, 7/7 unit tests. Admin-client allowlist (D-026) stays at 3 sites — no new uses introduced. |
| 2026-05-23 | **T-052 done.** GUTA pair linkage UI shipped on consignment detail (PRD §8.15). `consignments/[id]/page.tsx` fetches `guta_pairs` by id with user-bound `getSupabaseServerClient`, resolves the sibling via the non-matching of `parts_consignment_id`/`frames_consignment_id`, and filters with `.is("deleted_at", null)` so soft-deleted siblings hide the section. `consignment-detail.tsx` accepts a `gutaPair` prop (batch code, this-side role PARTS/FRAMES, sibling fields) and renders a new "GUTA pair" section between "Vessel & shipping" and "Linked EFD records" — indigo batch-code badge, red warning banner when `release_status === "Released"` differs between the two records (names the unreleased sibling by REF No), and a clickable sibling card with REF No, B/L, container count × type, amount, and release date. No new admin-client uses (D-026 allowlist stays at 3 sites). V-GUTA added to `validation.md`. Gates: typecheck clean, lint 0 errors / 6 pre-existing warnings, 7/7 unit tests. |
| 2026-05-23 | **T-053a done.** Alerts edge function code-complete (deploy is T-053b / H-010). New migration `20260523163840_stuck_alerts.sql` applied to dev: `stuck_alerts(consignment_id, stage, alerted_at, resolved_at)` table + two SECURITY DEFINER helpers — `claim_new_stuck_alerts()` (atomic INSERT … ON CONFLICT DO NOTHING RETURNING from `v_stuck_stages`; idempotent across retries) and `reset_resolved_stuck_alerts()` (DELETE ledger rows whose pair has left `v_stuck_stages`, so re-stuck cycles re-alert). Edge function at `supabase/functions/alerts/index.ts` (Deno + `Deno.serve`): `Authorization: Bearer ALERTS_CRON_SECRET` gate; calls reset → claim → if non-empty, resolves admins via `user_roles!inner(roles.name='admin')` + `auth.admin.getUserById`, POSTs one digest per admin to `https://api.resend.com/emails` directly via fetch (no SDK per D-033). Returns `{sent, claimed, reset, admins, errors}` JSON for cron logs. Decisions logged: D-031 (ledger model), D-032 (digest per admin), D-033 (Resend HTTP, no SDK). `tsconfig.json` + `eslint.config.mjs` exclude `supabase/functions/**` so the Deno runtime file doesn't poison the Next.js gates. Types regenerated via `pnpm gen:types:dev`. Gates: typecheck clean, lint 0 errors / 6 pre-existing warnings, 7/7 unit tests, migration applied + verified via `supabase migration list --linked`. T-053b (deploy + cron) is documented in `humanTasks.md` H-010. |
| 2026-05-23 | **T-055 done (hotfix).** Pre-existing T-042 filter bugs on `/consignments` surfaced when the new dashboard's "Pending release" KPI deep-linked to `?stage=unreleased`. Bug 1: `.neq("release_status", "Done")` → Postgres rejected because the enum has no `Done` (only `Waiting`/`Released`); fixed to `.neq("release_status", "Released")`. Bug 2: `?stage=stuck` was `.eq("release_status", "Waiting")` — returned every unreleased row, not the stuck subset; fixed to query `v_stuck_stages` and `.in("id", stuckIds)` with empty-set safety. Dropdown label updated: "Waiting (no action)" → "Stuck > 48h" so it matches both the filter behavior and the dashboard KPI. Gates: typecheck clean, lint 0 errors / 6 pre-existing warnings, 7/7 unit tests. |
| 2026-05-23 | **T-054 done.** Dashboard at `/dashboard` (PRD §6.3). Server component issues 7 reads in parallel via `Promise.all`: `v_pipeline_funnel` (year), released-today count, pending-release count, current-month revenue rows, this-week arrivals, top-5 clients from `v_client_volume`, top-10 stuck rows from `v_stuck_stages`, plus total-active count for the page header. 4 KPI tiles (Released today, Pending release, Stuck > 48h, Revenue · current month with compact tile + exact-figure footer); funnel renders 10 Action stages as relative-width bars (min 2%); top-5 client list with sub_label; arrivals this week uses Mon→Sun calendar window (inclusive Mon 00:00, exclusive next Mon 00:00) capped at 20; overdue jobs top-10 with `stageLabelFor()` mapping DB enum to UI label. Sidebar nav reordered — new Dashboard entry at `/dashboard`, `/` renamed to "Pipeline". No new admin-client uses (D-026 allowlist stays at 3 sites). V-DASH added to `validation.md`. Gates: typecheck clean, lint 0 errors / 6 pre-existing warnings, 7/7 unit tests; dev-server smoke confirmed the route compiles and `/dashboard` 307-redirects unauthenticated requests to `/login?next=/dashboard`. |
| 2026-05-24 | **T-061 done.** Excel import UI at `/import` (admin+operator gated). SheetJS adapter (`workbookToRows`) reads first sheet → `CellValue[][]` → `parseTracker`. Preview shows errors / warnings / auto-create chips / first-25 row sample. Confirm commits row-by-row via `commitImportAction` with case-insensitive `ilike` lookup + in-process cache for client/ICD resolution; missing clients/ICDs auto-created (D-037). One `import_jobs` audit row per attempt (preview inserts; commit updates). New migration `20260524181824_import_jobs.sql` applied to dev (table + audit trigger + RLS — admin SELECT all, operator SELECT own, admin+operator INSERT/UPDATE). `xlsx@0.18.5` added (server-only). Sidebar nav "Import" between EFD and Reports. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 35/35 unit tests, `pnpm build` clean (route compiles). Supabase advisors clean for `import_jobs` (no new findings). D-037 logged. |
| 2026-05-24 | **T-060 done.** Excel parser shipped as a pure function over `CellValue[][]` (D-035 — SheetJS deferred to T-061/T-062 adapters). Header-driven column resolution (D-036) with case + punctuation tolerance. Two-bucket output: `errors` block import, `warnings` still import. Covers every PRD §10.3 rule (year separators, Excel serial dates, decimal time, multi-EFD with `..NNN` abbreviated-prefix expansion, empty-row skip) + §8.20 REF No padding + §8.5 amount-band soft validation per container type/count + §8.19 TANSAD-missing warning. 28 Vitest cases (35 unit tests total, up from 7). Bug caught during test run: `normaliseHeader` only stripped trailing `.` — fixed to strip `.` and `:` anywhere so "ARR. DATE" / "TANSAD No." headers match aliases. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 35/35 unit tests. No new dep installed — `xlsx` (SheetJS) waits for T-061. |
| 2026-05-24 | **@dnd-kit SSR fix.** New `kanban-board-client.tsx` imports `KanbanBoard` via `next/dynamic({ ssr: false })`; `page.tsx` (server component) renders the shell. Eliminates the hydration warning at root (no `@dnd-kit` modules touched during SSR, so the `useSortable`/`useDroppable` counter can't mismatch). Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings (down from 6 — dropped unused `StageField` import), 7/7 unit tests. Also pays forward to T-086 (D-034) which branches on viewport at the same shell level. |
| 2026-05-24 | **D-034 logged + T-086 scheduled.** Mobile pipeline view will drop DnD entirely below `md`: sticky stage selector + single-stage vertical list + tap-card action sheet calling existing `advanceStageAction` / `forceSetStageAction`. T-080's scope narrowed to Inbox/Detail/Form (kanban carved out into T-086). No code yet — Phase 7 work. |
| 2026-05-23 | **T-051 done.** `in_ref` batch panel shipped + PRD §8.4 EFD-sibling auto-link wired. Right-side drawer at `/consignments` and `/consignments/[id]` driven by `?batch=&bc=&by=` URL params (shareable, browser-back closes); closes via X, Esc, backdrop. Built `_batch-panel/batch-panel.tsx` (client shell) + `_batch-panel/batch-panel-content.tsx` (server component fetching `v_in_ref_batches` + siblings) + `src/components/batch-link.tsx` (chip). IN REF column added to consignments table (lg breakpoint) and a Field on consignment detail Core details. `expandToBatchSiblings()` in `src/server/actions/efd.ts` auto-expands the selection to every consignment sharing `(in_ref, client_id, year)` from both `createEfdAction` and `linkConsignmentsAction` — idempotent via existing `ignoreDuplicates` upsert. Drawer's "Create EFD for this batch" CTA deep-links to `/efd/new?from_batch=&client=&year=` and the page pre-selects every sibling. Types regenerated via `pnpm gen:types:dev` (`v_in_ref_batches` now in `Database['public']['Views']`). V-BATCH added to `validation.md`. Gates: typecheck clean, lint 0 errors / 7 pre-existing warnings, 7/7 unit tests. Admin-client allowlist (D-026) stays at 3 sites. |
| 2026-05-26 | **T-070 done — Phase 6 begun.** Reports screen at `/reports` exposes all 6 T-024 reporting views. Server component reads `?report=…&year=…&from=…&to=…` and dispatches to the matching sub-renderer (Revenue Summary, Client Volume, Turnaround · by Client, Turnaround · by ICD, Pipeline Bottleneck, Pending Refunds). Client `reports-filter-bar.tsx` uses `useTransition` so report/year/date changes are non-blocking. **D-039 logged**: date range scoped to date-bearing reports (Revenue uses `month gte/lte`, Pending Refunds uses `release_date gte/lte`); year-grain reports disable the inputs with an inline "Date range not applicable" badge — surfaces the view's actual grain instead of pretending to filter. Each report has explicit empty / error / totals rows. Money via `formatTzs`, dates via `formatDate`. No new admin-client uses (D-026 allowlist stays at 3 sites). V-REPORTS added to `validation.md`. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 35/35 unit tests, `pnpm build` clean with `/reports` in the route manifest. |
| 2026-05-26 | **T-062 done — Phase 5 closed.** `scripts/import-tracker.ts` shipped (~520 lines, run via `pnpm import:tracker <file>`). Admin-client auth from `.env.local` (no login flow, bypasses RLS, designed for the bulk historical load — D-038). Dry-run is default; `--commit` requires typing `IMPORT` on stdin (`--yes` for non-interactive). `--no-auto-create` safety valve fails fast if any client/ICD would be auto-created (CLI-only knob). Banner labels target as `DEV` / `PROD` / `UNKNOWN` by comparing `NEXT_PUBLIC_SUPABASE_URL` against the project IDs in `package.json`. Dry-run dumps `tmp/import-preview-<iso>.json` for offline review. Commit branch INSERTs one `import_jobs` row up front (`payload.source='cli'`, `user_id=null` per D-038) and UPDATEs it to `committed`/`failed` after the row-by-row loop (mirrors `commitImportAction:254-372`). Exit codes 0/1/2 distinguish clean / setup-error / partial-failure. Side-effects: `package.json` gained `tsx@^4.19.2` devDep + `import:tracker` script; new `tmp/.gitignore`. D-038 logged. V-IMPORT-CLI (14 checks) added to `validation.md`. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 35/35 unit tests, `pnpm build` clean (script lives outside the Next.js bundle), `pnpm import:tracker --help` + bad-file dry-run verified against `kdl-tracker-dev` (DEV banner + zero DB writes confirmed via `select count(*) from import_jobs`). Full real-fixture commit deferred to ad-hoc QA on `fixtures/TRACKER -- KDL.xlsx` (file is gitignored). Phase 5 is now closed. |
| 2026-05-26 | **T-071 done.** XLSX export wired for all 6 reports on `/reports`. Each `ReportHeader` now carries an **Export XLSX** anchor (`<a href download>` — GET-based, browser-native download UX, no JS). New route handler at `src/app/api/reports/[kind]/xlsx/route.ts` (Node runtime, force-dynamic) validates `kind` against `REPORT_OPTIONS`, gates auth via `getServerPermissions()` (401 unauth; viewers + up download per V-REPORTS), parses `year`/`from`/`to` with the same ISO-date validator the page uses, dispatches to the new **shared** `fetchReportRows(kind, filters, supabase)` helper, then `buildReportWorkbook(...)` → `workbook.xlsx.writeBuffer()` → `Response`. The page's six inline view queries are now collapsed into the same `fetchReportRows` call so download bytes match rendered rows exactly. Workbook layout: row 1 title, row 2 generated-at + filter summary, row 3 blank, row 4 bolded + frozen headers, row 5+ data, totals row matching the page. **Money cells are numeric** (`numFmt "TSh"#,##0`) — sortable / summable in Excel; **date cells are real `Date`** (`numFmt yyyy-mm-dd`). Empty result writes `"No rows matched the filter."` at row 5 — never throws. Sheet names sanitised for Excel (strip `\ / ? * [ ] : ·`, cap 31). New files: `src/server/reports/{fetch-report-rows,build-xlsx,report-types}.ts` and `src/app/(app)/reports/report-options.ts` (extracted `REPORT_OPTIONS` + `ReportKind` out of the `"use client"` filter-bar after the first build failed on a client-reference-proxy at runtime). `reports-filter-bar.tsx` re-exports both for backward compat. `exceljs@4.4.0` added to deps. D-026 allowlist stays at 3 sites; D-035 invariant holds (no SheetJS import in any new file). `tests/unit/build-xlsx.test.ts` (+11 cases) — total Vitest count 46/46 (was 35/35). V-REPORTS-XLSX (14 checks) added to `validation.md`. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 46/46 unit tests, `pnpm build` clean with `/api/reports/[kind]/xlsx` listed in the route manifest. |
| 2026-05-31 | **T-081 done — security review.** Three parallel audit agents (RLS coverage, secret-bundle leakage, server-action auth gates) + direct source verification. **Posture confirmed solid:** RLS on all 14 user-facing tables; soft-delete-only (no hard-delete policies on operational tables); audit log append-only; admin-client allowlist = exactly the 3 D-026 sites; secret key sealed from the client bundle (`import "server-only"` in `admin.ts` + `server-only` dep + non-`NEXT_PUBLIC_` env access; no `"use client"` importer; zero hardcoded keys). Several agent "CRITICAL/HIGH" flags were **false positives** (advanceStageAction relies on the DB's D-029 caller gate; deleteRole is atomic via the `roles_prevent_system_mutation` trigger) — verified in source. **Real findings fixed:** (1) **D-046** — closed the `consignments_update` `with check (true)` per-column gap with migration `20260525090000_consignments_column_write_guard.sql` (BEFORE UPDATE trigger → `can_user_write()` per changed column → `42501`; `advance_stage`/`force_set_stage` re-emitted with a tx-local `app.bypass_column_guard` GUC; admins short-circuit via `is_admin()`); (2) the `in_ref_batch_id`→`in_ref` seed + roles-UI typo. Deliverable `audit_rls.sql` added at repo root. Applied to dev via `supabase db push` — first had to repair pre-existing migration-history drift (import_jobs was on remote as `20260524182018` vs local `20260524181824`; marked the stray reverted + the local applied). Types regenerated (`gen:types:dev`). New files: `supabase/migrations/20260525090000_consignments_column_write_guard.sql`, `audit_rls.sql`. Modified: `src/app/(app)/settings/roles/roles-client.tsx`, `src/types/supabase.ts`, `validation.md` (V-PERM + RLS-audit checks), `decisions.md` (D-046), `tasks.md` (T-081 done + new T-087 for the `efd_records` gap). Behavioural REST walkthrough (operator `42501` on `amount`) owed to ad-hoc QA. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 67/67 unit tests; migration recorded on both Local + Remote. |
| 2026-05-31 | **T-072 done — Phase 6 closed.** PDF export wired for all 6 reports on `/reports`. New `src/server/reports/build-pdf.tsx` (pure `@react-pdf/renderer` element builder, mirrors `build-xlsx.ts`): A4 landscape, fixed page header (Kingdao logo from `public/KINGDAO_LOGO.png` via `readFileSync(process.cwd()/KINGDAO_LOGO.png)` with blank-box fallback, title, `Generated … UTC`, filter summary) + footer `page / total`, both repeated per page via react-pdf `fixed`; per-report tables switched on `payload.kind`; money via `formatTzs`, dates `yyyy-mm-dd`; totals row on Revenue / Client Volume / Pipeline Bottleneck / Pending Refunds (none on the two Turnaround reports — matches page + XLSX); empty result → `"No rows matched the filter."`; data rows `wrap={false}` + header repeats on page breaks. Route handler `src/app/api/reports/[kind]/pdf/route.ts` (`runtime: "nodejs"`, `dynamic: "force-dynamic"`) mirrors the XLSX route exactly — validates `kind` vs `REPORT_OPTIONS`, gates on `getServerPermissions()` (401 unauth / 400 bad kind / 500 query error), reuses `fetchReportRows()` + `reportFilenameStem()`, `renderToBuffer()` → `new Uint8Array(buffer)` → `Response`. Modified `reports/page.tsx`: added `exportHrefPdf` to all 6 `ReportHeader` calls (prop was required but unpassed in the WIP — typecheck was red), dropped the stale T-072 footer note. New `tests/unit/build-pdf.test.ts` (+9 — six kinds render valid `%PDF`, empty + null-funnel payloads don't throw). One `jsx-a11y/alt-text` false positive on react-pdf `<Image>` suppressed with a one-line disable + comment. D-026 allowlist stays at 3 sites; D-035 holds (no SheetJS — builder imports only `@react-pdf/renderer` + `@/lib/money` + `./report-types`). `@react-pdf/renderer@^4.5.1` already in deps. V-REPORTS-PDF added to `validation.md`. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 67/67 unit tests, `pnpm build` clean with `/api/reports/[kind]/pdf` in the route manifest. |
| 2026-05-26 | **Perf sweep — D-040 → D-044.** Instrumented `[perf]` logging across middleware + layout + permissions + dashboard / consignments / kanban hot paths via new `src/lib/perf.ts` helper. Initial numbers showed every Supabase round-trip costing ~300ms (West Europe ↔ Dar latency floor, not DB work). Five cuts shipped together: **D-040** — middleware switched from unconditional `getUser()` to `getClaims()` fast path + near-expiry `getUser()` refresh only when `exp - now < 5 min`. **D-041** — new `src/lib/permissions-cache.ts` adds a module-scoped `Map<userId, CachedPermissions>` with 5-minute TTL on top of the existing React `cache()`; mutation sites in `settings-users.ts` (invite/assign/remove/ban/unban) and `settings-roles.ts` (column-perm edit, role delete) call `invalidatePermissionsCache(userId)` / `invalidatePermissionsCacheAll()`. **D-042** — `/consignments` data fetch restructured: default case fires `clients-dropdown` + main query as one `Promise.all`; `stage=stuck` case fires `clients-dropdown` + `v_stuck_stages` in parallel (tier 1), then the filtered main query (tier 2). **D-043** — `consignments-client.tsx` wraps every filter navigation in `useTransition` with a 60% opacity fade on the table + inline "Updating…" spinner; year tabs and pagination converted from `<a>` to `<Link>` (was triggering hard reloads — soft-nav bug fix). **D-044** — `/settings/users` inner+outer parallelization: `listUsersAction` now fires `auth.admin.listUsers` + `user_roles` query in `Promise.all`; the page fires `roles` dropdown + `listUsersAction` in `Promise.all`. New file: `src/lib/perf.ts`, `src/lib/permissions-cache.ts`. Modified: `src/middleware.ts`, `src/app/(app)/layout.tsx`, `src/lib/permissions.ts`, `src/app/(app)/dashboard/page.tsx`, `src/app/(app)/consignments/page.tsx`, `src/app/(app)/consignments/[id]/page.tsx`, `src/app/(app)/consignments/consignments-client.tsx`, `src/app/(app)/settings/users/page.tsx`, `src/server/actions/consignments.ts`, `src/server/actions/settings-users.ts`, `src/server/actions/settings-roles.ts`. Expected per-page cuts (measured via perf-log timings): `/` (kanban) 815ms → ~200ms (warm cache), `/consignments` 1267ms → ~370ms, `/dashboard` ~1000ms → ~400ms, `/settings/users` 2100ms → ~600ms. Trust model trade-off: a revoked session stays usable until the JWT expires (≤1 hour); permissions changes propagate within ≤5 minutes if invalidation is missed. Both bounded and acceptable for the ~10-user internal-staff scope. Gates: typecheck clean, lint 0 errors / 5 pre-existing warnings, 46/46 unit tests. |

---

## Open issues / known compromises

- **Operator + admin walkthrough still owed as ad-hoc QA.** T-048 was closed on code-level acceptance (viewer was verified DB-side 2026-05-22). Joined columns (`clients.name`, `icds.location`) resolve through user JWT after migration `025325` and were confirmed during the viewer walkthrough, but the operator/admin matrices haven't been clicked through end-to-end. Surface anything that breaks as a new task rather than re-opening T-048.
- ~~**Page latency** — Server-rendered pages currently make 3 serial `auth.getUser()` calls + 3 serial permission queries per request.~~ **Resolved by T-049 on 2026-05-22.** Layout now uses `getClaims()` (local verify), `getServerPermissions()` is `cache()`-memoised per request, and the role-lookup chain is 2 queries instead of 3. `application-code` time on `GET /` is now 31–169ms warm. Middleware's `proxy.ts` time (~300–1500ms in dev logs) is the remaining latency, dominated by the canonical `getUser()` call — that's the security-critical session refresh and stays.
- ~~**@dnd-kit hydration warning on kanban.**~~ **Resolved on 2026-05-24.** Wrapped `KanbanBoard` in a thin client shell (`kanban-board-client.tsx`) that imports the board via `next/dynamic({ ssr: false })`. `@dnd-kit` no longer mounts during SSR, so the module-level counter cannot mismatch. `page.tsx` (server component) renders the shell; the shell renders the board on the client only. Tradeoff: the board flashes a small "Loading board…" state on first mount; data fetch is unchanged (still server-side via `fetchKanbanData`, props serialized in). This also pays forward to T-086 (mobile pipeline view per D-034), which will branch on viewport at the same shell level.
- ~~**Column-level UPDATE policy is app-layer only.**~~ **Resolved by T-081 / D-046 on 2026-05-31.** Migration `20260525090000` adds a `BEFORE UPDATE` trigger (`consignments_enforce_column_write()`) that calls `can_user_write()` per changed column and raises `42501`; `advance_stage()`/`force_set_stage()` opt out via a tx-local GUC so the sanctioned pipeline writers still work. RLS now agrees with the UI per CLAUDE.md §3.3. **Remaining:** the identical `efd_records` `with check (true)` gap is logged as T-087 (needs a per-column seed first). And the behavioural REST walkthrough (operator gets `42501` on `amount`) is owed as ad-hoc QA — the migration applied cleanly but the runtime proof needs a logged-in operator JWT.

---

## Next 5 things, in order

Phase 4's T-053b stays at the bottom — no Supabase dashboard access right now. T-081 just shipped (security review + D-046 column-write guard). Phase 7 close-out continues.

1. **T-082** — CI on GitHub Actions: `pnpm typecheck` / `lint` / `test`, `supabase db reset` (fresh schema must apply from zero — this will also exercise migration `20260525090000`), `pnpm test:e2e`. Push to a branch triggers CI; all jobs green on a clean repo. Blocked by nothing.
2. **Ad-hoc QA — security walkthrough + exports + import.** (a) **V-PERM "Per-column UPDATE guard" (owed by T-081):** as an operator JWT, direct REST `PATCH consignments {"amount":999}` → expect `42501`; `{"remarks":"ok"}` → 200; `{"in_ref":"TZ9"}` → 200; `advance_stage` still works; admin `{"amount":500}` → 200. Run `audit_rls.sql` and eyeball Query 1 (all RLS on). (b) Download an XLSX **and PDF** for each of the 6 reports (V-REPORTS-XLSX + V-REPORTS-PDF — logo, pagination, totals, numeric formatting). (c) Upload `fixtures/TRACKER -- KDL.xlsx` as admin (or `pnpm import:tracker … --dry-run`), spot-check counts + auto-create chips. (d) Operator/admin/viewer click-through of `/reports`, `/dashboard`, `/efd`, batch drawer, GUTA section.
3. **T-083 (deploy)** — Vercel + Supabase Cloud. Apply migrations via `supabase db push`; set env vars in Vercel; admin login + one test consignment end-to-end. None blocked.
4. **T-087 (security follow-up)** — apply the D-046 per-column guard to `efd_records` (same `with check (true)` gap; needs a per-column seed first). Not blocking deploy.
5. **T-053b (Phase 4 straggler)** — once dashboard access is restored: H-004 (Resend), `supabase secrets set …` + `functions deploy alerts` + `*/30 * * * *` cron, then V-ALERT acceptance test. Steps in `humanTasks.md` H-010.

After this: T-084 (prod import) + T-085 (README/operator guide), and the *Released-but-no-EFD* dashboard widget (still needs a task ID) close out v1.

---

## Outstanding questions

None. All known questions are answered or scheduled as future tasks.

---

## How to update this file

After every completed task, append to "Recent activity" and update:
- the Snapshot table,
- the "Next 5 things" list,
- any new questions or risks.

Keep entries terse — one line each.
